{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "vite build && node -e \"require('fs').cpSync('dist/public','public',{recursive:true})\"",
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
